"# Plan Fix Bug & 20 Pro-Max Recommendations\n\n## Goal\nFix duplicate `view-transition-name` error and 500 API error on `/library?sort=title_asc`.\nProvide 20 Huashu/UI-UX Pro Max recommendations.\nCaveman mode active: *Me talk short. You read fast.*\n\n## Root Cause\n1. **Duplicate View Transition:** Shinigami API return duplicate manga in search/list. `library/page.tsx` map them without dedupe. Two DOM elements get same `view-transition-name`. Crash.\n2. **API 500 Error:** `LibraryContent` hardcode `sort` options (`title_asc`, `title_desc`). Shinigami API no support `sort=title`. Server crash when parsing or forwarding.\n\n## Proposed Changes\n\n### 1. Fix Library Page (`src/app/(web)/library/page.tsx`)\n- **[MODIFY]** Remove hardcoded `sort` options.\n- **[MODIFY]** Use `filtersData?.sorts` from API to render `<CustomSelect>`.\n- **[MODIFY]** Deduplicate `mangas` array before render so no duplicate `vtName`.\n- **[MODIFY]** Add fallback: if current `sort` URL param not in `filtersData.sorts`, fallback to `popular`.\n\n## 20 Recommendations (UI/UX Pro Max, Huashu, Perf)\n\n1. **JS Hydration Shift:** `manga-detail-view.tsx` use `isDesktop` for view transition. Bad. Use CSS `@media` to show/hide desktop/mobile DOM. No JS jump.\n2. **Double Render:** `useMounted` in `manga-card.tsx` cause double render for `isInLibrary`. Use Zustand persist hydration pattern.\n3. **Image Cache Bug:** `onError` in `<img/>` fail if broken image already cached. Add `ref` and check `complete` property.\n4. **View Transition Scope:** Only apply `view-transition-name` on `onPointerDown` or via global state. If many items have `vtName` on screen, browser slow.\n5. **Concurrent React:** Swap `useDebounce` with `useDeferredValue` for search input. UI feel 10x faster.\n6. **Smart Scroll:** `window.scrollTo({ top: 0 })` jump too far. Use `useRef` to scroll to grid top only.\n7. **Virtualize Grid:** If API return 100 items, DOM heavy. Use `@tanstack/react-virtual` for windowing.\n8. **Heavy Blur:** `manga-detail-view.tsx` background `blur-3x
<truncated 1825 bytes>
