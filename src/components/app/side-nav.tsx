import * as React from "react";

/**
 * File ini sudah di-deprecate (tidak digunakan lagi) sejak kita beralih 
 * ke desain TopNav (Apple Glass style) dan Bottom Dock.
 * Komponen ini sengaja dibiarkan me-return null agar error di IDE menghilang.
 */
export function SideNav() {
  return null;
}
